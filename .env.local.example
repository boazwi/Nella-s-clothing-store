# ---------------------------------------------------------------------------
# Nella's Clothing Store — environment variables
# Copy this file to `.env.local` and fill in real values. Never commit .env.local.
# ---------------------------------------------------------------------------

# Server-only (NOT exposed to the browser). Used by app/api/try-on/route.ts to
# forward the two images to the n8n virtual try-on webhook.
# Because it has no NEXT_PUBLIC_ prefix, it is never bundled into client code.
TRYON_WEBHOOK_URL=https://boazwi.app.n8n.cloud/webhook/bae811b4-033e-45f8-b7b0-026876b09bc8
